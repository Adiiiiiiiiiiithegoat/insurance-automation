# --- Secrets / credentials ---
.env
.env.*
!.env.example
deploy-token.txt

# --- Browser automation profile (cookies, sessions, cache — sensitive & huge) ---
automation_profile/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# --- Claude Code local settings ---
.claude/settings.local.json

# --- OS / editor cruft ---
Thumbs.db
.DS_Store
*.swp
*.lnk

# Screenshots may contain customer PII — never commit
screenshots/

# Downloaded customer documents (Civil ID / licence images) — never commit
iran_uploads/

# Runtime-generated log of unseen NI body types (regenerated each run)
unmatched_body_types.json
